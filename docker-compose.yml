version: '3.8'

services:
  app:
    build: .
    container_name: painel-lovable-ultra-chat
    ports:
      - "3000:3000"
    environment:
      - VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
      - VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY}
      - VITE_API_URL=${VITE_API_URL:-http://localhost:3000}
    restart: unless-stopped
    networks:
      - lovable-network

networks:
  lovable-network:
    driver: bridge
